{
  "name": "ovh-warp10-datasource",
  "version": "3.0.0",
  "description": "Warp 10™ datasource Grafana plugin",
  "main": "module.ts",
  "repository": "git@github.com:ovh/ovh-warp10-datasource.git",
  "author": "ovh <contact@metrics.ovh.net>",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=7.9.0"
  },
  "scripts": {
    "docker": "docker build -t grafana . && docker run -i -p 3000:3000 grafana",
    "build": "grafana-toolkit plugin:build",
    "test": "grafana-toolkit plugin:test",
    "dev": "grafana-toolkit plugin:dev",
    "watch": "grafana-toolkit plugin:dev --watch"
  },
  "devDependencies": {
    "@grafana/data": "next",
    "@grafana/runtime": "next",
    "@grafana/toolkit": "next",
    "@grafana/ui": "next",
    "@monaco-editor/react": "3.3.0",
    "monaco-editor": "^0.20.0",
    "@types/lodash": "latest"
  }
}
